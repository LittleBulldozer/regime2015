using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class BarContents : MonoBehaviour
{
    float ratio = 0.0f;

	// Update is called once per frame
	void Update () {
//        ratio = Mathf.Min(1.0f, ratio + 0.1f * Time.deltaTime);

//        GetComponent<Image>().fillAmount = ratio;
    }
}
